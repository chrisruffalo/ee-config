# versions for filtering through the deployment manager
# so that we can auto-update the deployment provider
# without doing it manually
jee6.version=${jee6.version}
jboss-as.version=${jboss-as.version}
slf4j.version=${slf4j.version}
guava.version=${guava.version}
commons.configuration.version=${commons.configuration.version}